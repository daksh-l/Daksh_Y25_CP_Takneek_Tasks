Standard DP problem where we use memoisation to call functions instead of calculating them again (similar to Fibonacci numbers).

defined an array called memo where we store the minimum cost to reach a particular stone so that we dont need to calculate it each time.